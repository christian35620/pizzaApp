// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    firebase: {
        apiKey: "AIzaSyDQ0s3Uoeuy4G0ZSVCGRPlU0b-Au6uI5DE",
        authDomain: "pizzapp-5d038.firebaseapp.com",
        databaseURL: "https://pizzapp-5d038.firebaseio.com",
        projectId: "pizzapp-5d038",
        storageBucket: "pizzapp-5d038.appspot.com",
        messagingSenderId: "578804586890",
        appId: "1:578804586890:web:d8809a54788a11c3d21073",
    },
}

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
